
You said:
# توثيق ميزات المشروع (Project Features Documentation)

## نظرة عامة
هذا مشروع متجر إلكتروني (E-Commerce) مبني باستخدام React و Vite مع Firebase للمصادقة وقاعدة البيانات.

---

## 📦 الميزات الرئيسية (Main Features)

### 1. **نظام المصادقة (Authentication System)**
**الوصف:** نظام تسجيل دخول وتسجيل حساب جديد باستخدام Firebase Authentication.

**كيفية الاستخدام:**
- **التسجيل (Signup):** في صفحة /signup، المستخدم يملأ:
  - الاسم الأول (FirstName)
  - الاسم الأخير (LastName)
  - رقم الهاتف (Phone Number) - يجب أن يبدأ بـ 01 ويحتوي على 11 رقم
  - البريد الإلكتروني (Email)
  - كلمة المرور (Password) - يجب أن تحتوي على 6 أحرف على الأقل، حرف كبير واحد، ورمز خاص واحد
  - تأكيد كلمة المرور (Confirm Password)
- **تسجيل الدخول (Login):** في صفحة /login، المستخدم يدخل:
  - البريد الإلكتروني
  - كلمة المرور

**الملفات المستخدمة:**
- src/context/AuthContext.jsx - Context لإدارة حالة المستخدم
- src/pages/Auth/Login.jsx - صفحة تسجيل الدخول
- src/pages/Auth/Signup.jsx - صفحة التسجيل
- src/validation/loginValidation.js - التحقق من صحة بيانات تسجيل الدخول
- src/validation/regiserValidation.js - التحقق من صحة بيانات التسجيل
- src/fireBase/Firebase.js - إعدادات Firebase

**المكتبات المستخدمة:**
- firebase/auth - للمصادقة
- firebase/firestore - لحفظ بيانات المستخدم
- react-hook-form - لإدارة النماذج
- zod - للتحقق من صحة البيانات
- @hookform/resolvers - لربط Zod مع React Hook Form

---

### 2. **نظام السلة (Shopping Cart System)**
**الوصف:** نظام لإدارة المنتجات في سلة التسوق مع إمكانية إضافة وحذف وتعديل الكمية.

**كيفية الاستخدام:**
- **إضافة منتج للسلة:** من أي بطاقة منتج، اضغط على أيقونة السلة
- **زيادة/تقليل الكمية:** في صفحة /cart، استخدم الأزرار + و -
- **حذف منتج:** في صفحة /cart، اضغط على "Remove"
- **عرض السلة:** اضغط على أيقونة السلة في الشريط الجانبي أو اذهب إلى /cart

**الملفات المستخدمة:**
- src/context/CartContext.jsx - Context لإدارة السلة
- src/pages/Cart/Cart.jsx - صفحة السلة
- src/component/ShortCart/ShortCart.jsx - سلة صغيرة جانبية
- src/common/Product/Product.jsx - بطاقة المنتج مع زر إضافة للسلة

**المكتبات المستخدمة:**
- axios - للتواصل مع JSON Server
- json-server - قاعدة بيانات محلية للاختبار

**المميزات:**
- حفظ السلة لكل مستخدم بشكل منفصل
- حساب المجموع الكلي تلقائياً
- رسوم الشحن ($6.90)
- إمكانية تطبيق كوبون خصم (واجهة فقط)

---

### 3. **قائمة الأمنيات (Wishlist System)**
**الوصف:** نظام لحفظ المنتجات المفضلة للمستخدم.

**كيفية الاستخدام:**
- **إضافة/إزالة من الأمنيات:** اضغط على أيقونة القلب في بطاقة المنتج
- **عرض قائمة الأمنيات:** اذهب إلى صفحة /wishlist
- **الشرط:** يجب تسجيل الدخول أولاً

**الملفات المستخدمة:**
- src/context/WishlistContext.jsx - Context لإدارة قائمة الأمنيات
- src/pages/Wishlist/Wishlist.jsx - صفحة قائمة الأمنيات
- src/common/Product/Product.jsx - بطاقة المنتج مع زر الأمنيات

**المميزات:**
- حفظ قائمة الأمنيات لكل مستخدم بشكل منفصل
- إشعارات Toast عند الإضافة/الإزالة
- تحذير للمستخدمين غير المسجلين

---

### 4. **عرض المنتجات (Product Display)**
**الوصف:** عرض المنتجات في صفحات مختلفة مع إمكانية التصفية والبحث.

**كيفية الاستخدام:**
- **الصفحة الرئيسية:** تعرض منتجات متنوعة في أقسام مختلفة
- **صفحة المتجر:** (قيد التطوير) /shop
- **بطاقة المنتج:** تعرض:
  - صورة المنتج
  - العنوان
  - السعر
  - التقييم (نجوم)
  - أزرار: إضافة للسلة، إضافة للأمنيات، معاينة، مقارنة

**الملفات المستخدمة:**
- src/context/DisplayContext.jsx - Context لإدارة بيانات المنتجات
- src/common/Product/Product.jsx - مكون بطاقة المنتج
- src/component/HomeProducts/HomeProducts.jsx - عرض المنتجات في الصفحة الرئيسية
- src/component/product1/product1.jsx - قسم منتجات 1
- src/component/product2/product2.jsx - قسم منتجات 2

**المكتبات المستخدمة:**
- axios - لجلب البيانات من JSON Server

---

### 5. **التصفح والتنقل (Navigation & Routing)**
**الوصف:** نظام تنقل بين الصفحات مع Layout مشترك.

**الصفحات المتاحة:**
- / - الصفحة الرئيسية (Home)
- /login - تسجيل الدخول
- /signup - التسجيل
- /cart - السلة
- /wishlist - قائمة الأمنيات
- /shop - المتجر (قيد التطوير)

**الملفات المستخدمة:**
- src/App.jsx - إعدادات Routing
- src/layout/Layout.jsx - Layout مشترك (Navbar + Footer)
- src/common/NavBar/Navbar.jsx - شريط التنقل الرئيسي
- src/common/Footer/Footer.jsx - التذييل

**المكتبات المستخدمة:**
- react-router-dom - للتنقل بين الصفحات
- lazy و Suspense - لتحميل الصفحات بشكل كسول (Lazy Loading)

---

### 6. **الرسوم المتحركة Lottie (Lottie Animations)**
**الوصف:** استخدام رسوم متحركة Lottie لشاشات التحميل والأخطاء.

**كيفية الاستخدام:**
- **شاشة التحميل الرئيسية:** تظهر عند تحميل الصفحة الرئيسية (stat='main')
- **شاشة التحميل العامة:** تظهر عند تحميل أي صفحة (stat='mshmain')
- **صفحة الخطأ 404:** تظهر عند حدوث خطأ (stat='err')

**الملفات المستخدمة:**
- src/common/lottieHandler.jsx - معالج الرسوم المتحركة
- src/assets/lottifiles/Loading.json - ملف التحميل 1
- src/assets/lottifiles/loading (1).json - ملف التحميل 2
- src/assets/lottifiles/Error 404.json - ملف خطأ 404

**المكتبات المستخدمة:**
- lottie-react - لعرض رسوم Lottie

---

### 7. **عرض الصفقات والعروض (Deals & Offers)**
**الوصف:** عرض عروض اليوم مع عداد تنازلي.

**كيفية الاستخدام:**
- **قسم "Deal Of The Day":** في الصفحة الرئيسية
- **السلايدر:** يمكن التنقل بين العروض
- **العداد التنازلي:** يعرض الوقت المتبقي للعرض

**الملفات المستخدمة:**
- src/component/DealDay/DealDay.jsx - مكون عروض اليوم

**المكتبات المستخدمة:**
- react-countdown - للعداد التنازلي
- swiper - للسلايدر

**المميزات:**
- عرض نسبة البيع (Progress Bar)
- عدد المنتجات المباعة
- السعر الأصلي والمخفض

---

### 8. **الأزرار الجانبية (Scroll Buttons & Settings)**
**الوصف:** أزرار للصعود للأعلى وإعدادات الموقع (اللون والثيم).

**كيفية الاستخدام:**
- **زر الصعود للأعلى:** يظهر بعد التمرير 300px، اضغط للصعود
- **زر الإعدادات:** اضغط على أيقونة الترس لفتح الإعدادات:
  - **تغيير اللون الرئيسي:** اختر لون من Color Picker
  - **تغيير الثيم:** اختر Dark أو Light
  - **RTL/LTR:** (واجهة فقط - غير مفعل)

**الملفات المستخدمة:**
- src/component/ScrollButtons/ScrollButtons.jsx - مكون الأزرار الجانبية

**المميزات:**
- حفظ الإعدادات في LocalStorage
- تطبيق اللون والثيم فوراً
- تصميم متجاوب

---

### 9. **التصنيفات (Categories)**
**الوصف:** عرض تصنيفات المنتجات المختلفة.

**كيفية الاستخدام:**
- **في الصفحة الرئيسية:** قسم التصنيفات يعرض جميع التصنيفات المتاحة
- **التصنيفات:** يتم جلبها تلقائياً من بيانات المنتجات

**الملفات المستخدمة:**
- src/component/Categories/Categories.jsx - مكون التصنيفات

---

### 10. **التحقق من صحة البيانات (Form Validation)**
**الوصف:** نظام تحقق شامل من صحة بيانات النماذج.

**قواعد التحقق:**

**تسجيل الدخول:**
- البريد الإلكتروني: مطلوب، يجب أن يكون بريد إلكتروني صحيح
- كلمة المرور: مطلوبة

**التسجيل:**
- الاسم الأول: مطلوب
- الاسم الأخير: مطلوب
- رقم الهاتف: مطلوب، يجب أن يبدأ بـ 01 ويحتوي على 11 رقم
- البريد الإلكتروني: مطلوب، يجب أن يكون بريد إلكتروني صحيح
- كلمة المرور: 
  - مطلوبة
  - 6 أحرف على الأقل
  - حرف كبير واحد على الأقل
  - رمز خاص واحد على الأقل
- تأكيد كلمة المرور: يجب أن تطابق كلمة المرور

**الملفات المستخدمة:**
- src/validation/loginValidation.js - قواعد تسجيل الدخول
- src/validation/regiserValidation.js - قواعد التسجيل

**المكتبات المستخدمة:**
- zod - للتحقق من صحة البيانات
- @hookform/resolvers - لربط Zod مع React Hook Form

---

### 11. **الإشعارات (Notifications)**
**الوصف:** نظام إشعارات للمستخدم.

**كيفية الاستخدام:**
- **Toast Notifications:** تظهر تلقائياً عند:
  - إضافة منتج للأمنيات
  - إزالة منتج من الأمنيات
- **SweetAlert:** تظهر عند:
  - محاولة استخدام ميزة تتطلب تسجيل الدخول

**المكتبات المستخدمة:**
- react-hot-toast - للإشعارات السريعة
- sweetalert2 - للتنبيهات التفاعلية

---

### 12. **الرسوم المتحركة للمنتجات (Product Animations)**
**الوصف:** رسوم متحركة عند إضافة منتج للسلة.

**كيفية الاستخدام:**
- عند الضغط على "إضافة للسلة"، تظهر صورة المنتج تتحرك من مكانها إلى أيقونة السلة الجانبية

**الملفات المستخدمة:**
- src/common/Product/Product.jsx - دالة imageAnimate()

---

### 13. **التصميم المتجاوب (Responsive Design)**
**الوصف:** الموقع متجاوب مع جميع أحجام الشاشات.

**المكتبات المستخدمة:**
- tailwindcss - لإطار عمل CSS
- @tailwindcss/vite - للتكامل مع Vite

**المميزات:**
- تصميم متجاوب للجوال والتابلت والكمبيوتر
- استخدام Breakpoints في Swiper
- Grid متجاوب للمنتجات

---

### 14. **قاعدة البيانات (Database)**
**الوصف:** استخدام JSON Server كقاعدة بيانات محلية للاختبار.

**كيفية الاستخدام:**
- **تشغيل JSON Server:** npm start (يستخدم db.json)
- **API Endpoints:**
  - GET /products - جلب جميع المنتجات
  - GET /cart?userid=... - جلب سلة المستخدم
  - POST /cart - إضافة منتج للسلة
  - DELETE /cart/:id - حذف منتج من السلة
  - GET /wishlist?userid=... - جلب قائمة أمنيات المستخدم
  - POST /wishlist - إضافة منتج للأمنيات
  - DELETE /wishlist/:id - حذف منتج من الأمنيات

**الملفات المستخدمة:**
- app/db.json - ملف قاعدة البيانات

**المكتبات المستخدمة:**
- json-server - لإنشاء API محلي
- axios - للتواصل مع API

---

## 🛠️ التقنيات المستخدمة (Technologies Used)

### Frontend Framework
- **React 19.2.0** - مكتبة JavaScript لبناء واجهات المستخدم
- **Vite 7.1.7** - أداة بناء سريعة

### Routing
- **react-router-dom 7.9.1** - للتنقل بين الصفحات

### State Management
- **React Context API** - لإدارة الحالة العامة:
  - AuthContext - حالة المستخدم
  - CartContext - حالة السلة
  - WishlistContext - حالة قائمة الأمنيات
  - DisplayContext - حالة المنتجات

### Forms & Validation
- **react-hook-form 7.64.0** - لإدارة النماذج
- **zod 4.1.12** - للتحقق من صحة البيانات
- **@hookform/resolvers 5.2.2** - لربط Zod مع React Hook Form

### UI Libraries
- **@mui/material 7.3.4** - مكونات Material-UI
- **react-icons 5.5.0** - مكتبة الأيقونات
- **tailwindcss 4.1.13** - إطار عمل CSS
- **styled-components 6.1.19** - CSS-in-JS

### Animations
- **lottie-react 2.4.1** - رسوم متحركة Lottie
- **framer-motion 12.23.22** - مكتبة الرسوم المتحركة

### Backend & Database
- **Firebase 12.3.0** - للمصادقة وقاعدة البيانات
- **json-server 0.17.4** - قاعدة بيانات محلية للاختبار
- **axios 1.13.1** - للطلبات HTTP

### Other Libraries
- **swiper 12.0.2** - للسلايدر
- **react-countdown 2.3.6** - للعداد التنازلي
- **react-hot-toast 2.6.0** - للإشعارات
- **sweetalert2 11.26.3** - للتنبيهات

---

## 📁 هيكل المشروع (Project Structure)

app/
├── src/
│   ├── assets/          # الصور والملفات الثابتة
│   ├── common/          # المكونات المشتركة
│   │   ├── Footer/
│   │   ├── NavBar/
│   │   ├── PageHeader/
│   │   ├── Product/
│   │   └── lottieHandler.jsx
│   ├── component/       # المكونات الخاصة بالصفحات
│   │   ├── BestValue/
│   │   ├── Categories/
│   │   ├── DealDay/
│   │   ├── Header/
│   │   ├── HomeProducts/
│   │   ├── ScrollButtons/
│   │   └── ShortCart/
│   ├── context/        # Context API
│   │   ├── AuthContext.jsx
│   │   ├── CartContext.jsx
│   │   ├── DisplayContext.jsx
│   │   └── WishlistContext.jsx
│   ├── fireBase/       # إعدادات Firebase
│   ├── hooks/          # Custom Hooks
│   ├── layout/         # Layout المشترك
│   ├── pages/          # الصفحات
│   │   ├── About/
│   │   ├── Auth/
│   │   ├── Cart/
│   │   ├── Contact/
│   │   ├── Home/
│   │   ├── Shop/
│   │   └── Wishlist/
│   ├── validation/     # قواعد التحقق
│   ├── App.jsx         # المكون الرئيسي
│   └── main.jsx        # نقطة الدخول
├── db.json             # قاعدة البيانات المحلية
└── package.json        # التبعيات


---

## 🚀 كيفية التشغيل (How to Run)

1. **تثبيت التبعيات:**
   
bash
   npm install


2. **تشغيل JSON Server:**
   
bash
   npm start


3. **تشغيل المشروع:**
   
bash
   npm run dev


4. **بناء المشروع للإنتاج:**
   
bash
   npm run build


---

## 📝 ملاحظات مهمة (Important Notes)

1. **Firebase Configuration:** يجب تحديث إعدادات Firebase في src/fireBase/Firebase.js بمعلومات مشروعك الخاص.

2. **JSON Server:** يجب تشغيل JSON Server على http://localhost:3000 قبل استخدام الموقع.

3. **Authentication:** بعض الميزات (السلة، الأمنيات) تتطلب تسجيل الدخول.

4. **Lazy Loading:** الصفحات محملة بشكل كسول لتحسين الأداء.

5. **Error Handling:** يوجد معالجة للأخطاء في معظم العمليات.

---
